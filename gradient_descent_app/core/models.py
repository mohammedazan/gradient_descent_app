from django.db import models

# Create your models here.

# For this application, we're using session-based storage
# so we don't need any database models.
# All data is stored temporarily in the session and files.